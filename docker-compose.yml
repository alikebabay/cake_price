
services:
  cake_price:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - dotenv_cake.env
    environment:
      - DB_PATH=/data/exchange_rates.db
      - DB_BACKEND=sqlite
    volumes:
      - exchange_rates_data:/data
    restart: unless-stopped

volumes:
  exchange_rates_data: